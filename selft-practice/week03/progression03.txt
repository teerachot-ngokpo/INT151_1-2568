week03 
เป็นการบ้านที่อาจารให้โดยโจทย์คือ
นับคำจำนวนคำในประโยครวมคำที่ซ้ำกันแล้วแปลงอยู่ในรูปของ object
และมีให้ return undefined ถ้าค่าเป็น null หรือ undefined
โดยใน code ผมได้เขียน if อันแรกเพื่อ check type ของ ประโยค
ว่าเป็น null หรือ underfined
แล้วสร้างตัวแปร words มาแปลงค่าประโยคให้อยู่ในรูปของ array โดยการ split และ
แปลงเป็นเป็นตัวเล็ก ด้วย toLowerCase() 
จากนั้นสร้างตัวแปร freq เป็น obj เปล่า
จากนั้น loop โดยใช้ for และสร้างตัวแปร word เพื่อมาเป็นตัวแทนค่าใน array ของ words อีกที
ภายใน loop สร้าง if เพื่อเช็คค่าโดย ถ้า freq[word] เป็น undefiend หรือคือไม่เคยเจอตัวคำนั้น
freq[word] จะเท่ากับ 1 แต่ถ้าไม่ใช่ให้ +1 freq[word]คำนั้น
หลังจากจบ loop ให้ return freq